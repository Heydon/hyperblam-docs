import { PropParamMethod } from '../helpers/PropParamMethod.js';
import { define } from '../helpers/define.js';

class Blam extends PropParamMethod {
  static {
    define(this);
  }
}

export { Blam }