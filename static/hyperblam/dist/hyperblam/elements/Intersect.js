import{Base}from"../primitives/Base.js";import{define}from"../tools/define.js";class Intersect extends Base{buildThresholds(steps){let thresholds=[];for(let i=1;i<=steps;i++){let ratio=i/steps;thresholds.push(ratio)}return thresholds.push(0),thresholds}handler(entries){let intersecting=entries[0].isIntersecting,change=intersecting===this.wasIntersecting;this.fire("blam",{x:entries[0].intersectionRatio,on:intersecting,switch:change},this),this.wasIntersecting=intersecting}get root(){return this.getAttribute("root")}set root(value){value&&this.setAttribute("root",value)}get steps(){let value=this.getAttribute("steps");return value?parseFloat(value):10}set steps(value){value&&this.setAttribute("steps",value)}get margin(){return this.getAttribute("margin")||"0px"}set margin(value){value&&this.setAttribute("margin",value)}connectedCallback(){let options={rootMargin:this.margin,threshold:this.buildThresholds(this.steps)};this.root&&(options.root=document.querySelector(this.root)),this.observer=new IntersectionObserver(entries=>{this.handler(entries)},options),this.observer.observe(this)}static{define(this)}}export{Intersect};